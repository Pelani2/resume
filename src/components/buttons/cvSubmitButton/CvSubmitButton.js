import React from "react";
import "./CvSubmitButtonStyles.css";

export default function CvSubmitButton({
    text, type
}) {
    return(
        <button>
            {text}
        </button>
    );
}